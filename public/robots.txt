# This is the SECONDARY (Lovable) property, not the canonical growmaticsa.com
# site (that is the hand-built `growmatic-sa` repo). It is intentionally kept
# out of search so it never competes with the real site for the domain.
User-agent: *
Disallow: /
